# HTMLTripPlanWriter
- Outputs a travel plan as an HTML file
- Takes TTRavelPlan and writes it into CDataSink as HTML
- Needs text and SVG trip plan writer implementations to create output
- Takes CStreetMap and CBusSystem and renders a complete HTML page
